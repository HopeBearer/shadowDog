const config = {
  //lives default is 5
  lives: 15,
  // the lower the number, the more the enemy there are
  enemyInterval: 800,
  // level 2 and level 3 enemyIntervals generate multiplications
  enemyInterval2: 500,
  enemyInterval3: 300,
  //speed
  speed: 2,
  //the number of seconds after which level 2 is enabled
  level2Time: 5,
  //the number of seconds after which level 3 is enabled
  level3Time: 10
};
export default config;
